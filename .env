PORT=9002
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=HydotTech
DB_NAME=ImageSearch

# Embedding settings
EMBEDDING_MODE=local      # "local" = CLIP image embeddings | "text" = old filename-based embeddings
SIMILARITY_THRESHOLD=0.85
SIMILARITY_METRIC=cosine  # or "euclidean"


